{
  "name": "tat",
  "private": true,
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"npm start --prefix client\" \"dotnet run --project server\""
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
