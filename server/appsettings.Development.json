{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AzureSync": {
    "_comment": "Used only by DevToolsController's local 'Sync DB from Live' button — never active outside Development. Credentials aren't stored here; fetched fresh via the Azure CLI each time.",
    "WebAppName": "tat",
    "ResourceGroup": "tat-rg",
    "RemoteDbPath": "data/tat.db"
  }
}
